'use strict'

const activeText = '#000'
const inactiveText = 'rgba(0, 0, 0, 0.44)'
const successText = '#069600'
const dangerText = '#FF0000'
const invertedText = '#FFF'

export {activeText, inactiveText, successText, dangerText, invertedText}
